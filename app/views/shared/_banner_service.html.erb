<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path service.photo.key, height: 300, crop: :fill %>')">
  <div class="container">
    <h1><%= service.skill_name %></h1>
    <p><%= @service.description %></p>
    <h3 style="color: white"><%= "($#{service.price_per_hour})" %></h3>
  </div>
</div>
