<!DOCTYPE html>
<html>
  <head>
    <title>Skillbnb</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>
  <div>
    <%# <p class="notice"><%= notice</p> %>
    <% unless alert.nil? %>
      <p class="alert alert-dark"><%= alert %></p>
    <% end %>
   </div>

   <div class= "page-container">
      <div class="navbar-container"><%= render "shared/navbar" %></div>
      <div class="content-container">
        <%= yield %>
      </div>
      <div class="content-container container"><%= yield %></div>
      <div class="footer-container"><%= render "shared/footer" %></div>
   </div>
  </body>

</html>
